development:
  adapter: mysql
  encoding: utf8
  database: tsa_dev
  pool: 5
  username: root
  password:
  host: localhost

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
    adapter: mysql
    encoding: utf8
    database: tsa_test
    pool: 5
    username: root
    password:
    host: localhost

production:
    adapter: mysql
    encoding: utf8
    database: twitter_search_app_production
    pool: 5
    username: root
    password:
    host: localhost
